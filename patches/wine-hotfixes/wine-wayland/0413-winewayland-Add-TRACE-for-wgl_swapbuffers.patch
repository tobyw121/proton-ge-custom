From e954240a03156f9ea53a38e3be15b1726eabbd1f Mon Sep 17 00:00:00 2001
From: Etaash Mathamsetty
 <45927311+Etaash-mathamsetty@users.noreply.github.com>
Date: Fri, 19 Dec 2025 22:38:53 -0500
Subject: [PATCH 413/486] winewayland: Add TRACE for wgl_swapbuffers.

---
 dlls/winewayland.drv/opengl.c | 3 +++
 1 file changed, 3 insertions(+)

diff --git a/dlls/winewayland.drv/opengl.c b/dlls/winewayland.drv/opengl.c
index 30bbedcc400..933d02c31f1 100644
--- a/dlls/winewayland.drv/opengl.c
+++ b/dlls/winewayland.drv/opengl.c
@@ -35,6 +35,7 @@
 #if defined(SONAME_LIBEGL) && defined(HAVE_LIBWAYLAND_EGL)
 
 WINE_DEFAULT_DEBUG_CHANNEL(waylanddrv);
+WINE_DECLARE_DEBUG_CHANNEL(opengl);
 
 #include <wayland-egl.h>
 #include <EGL/egl.h>
@@ -727,6 +728,8 @@ static BOOL wayland_wglSwapBuffers(HDC hdc)
     HWND hwnd = NtUserWindowFromDC(hdc), toplevel = NtUserGetAncestor(hwnd, GA_ROOT);
     struct wayland_gl_drawable *gl;
 
+    TRACE_(opengl)("hdc=%p\n", hdc);
+
     if (!(gl = wayland_gl_drawable_get(NtUserWindowFromDC(hdc), hdc))) return FALSE;
 
     if (ctx) wgl_context_refresh(ctx);
-- 
2.53.0

